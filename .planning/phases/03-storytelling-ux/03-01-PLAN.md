---
wave: 1
depends_on: []
files_modified:
  - src/pages/menu.astro
  - src/styles/global.css
  - src/components/MenuList.astro
  - src/components/Header.astro
autonomous: true
requirements:
  - STORY-01
  - STORY-02
  - PACE-02
---

# Phase 03: Storytelling & UX - Plan

This final phase focuses on the "heart" of the menu: adding narrative content, a visual Bento grid of Mediterranean imagery, and refined UX animations to create a warm, convivial experience.

## Task XML

<task type="auto">
  <name>Implement Narrative Introduction</name>
  <files>
    - src/pages/menu.astro
  </files>
  <action>
    1. Add a new section below the H1 title in `menu.astro`.
    2. Insert the localized narrative text:
        - IT: "Cucinare per qualcuno è un atto d'amore. Benvenuti al nostro tavolo, dove i sapori dell'Egeo incontrano il calore di Bologna. Lasciatevi guidare dai profumi e dalla tradizione."
        - EN: (Wait, I need to handle EN collection if it exists, otherwise just it for now).
    3. Style with `italic`, `font-marcellus`, `text-slate/70`, `max-w-2xl`, and `mx-auto`.
  </action>
  <verify>
    The intro text should appear centered below the main title, feeling warm and welcoming.
  </verify>
  <done>Narrative introduction implemented.</done>
</task>

<task type="auto">
  <name>Create Bento Imagery Grid</name>
  <files>
    - src/pages/menu.astro
  </files>
  <action>
    1. Implement a Bento-style grid using Tailwind (grid-cols and span classes).
    2. Use high-quality Unsplash URLs for Mediterranean visuals (Greek chairs, mezze table, coastal view).
    3. Ensure images have `aspect-video` or `aspect-square`, `object-cover`, and slight `border-radius` (irregular organic edges if possible).
    4. Position it between the intro text and the category navigation.
  </action>
  <verify>
    A visually stunning grid of Greek-inspired images should be visible on the menu page.
  </verify>
  <done>Bento imagery grid implemented with evocative Mediterranean visuals.</done>
</task>

<task type="auto">
  <name>Refine UX Animations (Fade-in-up)</name>
  <files>
    - src/components/MenuList.astro
    - src/styles/global.css
  </files>
  <action>
    1. Add a `fade-in-on-scroll` class or use the existing `animate-fade-in-up` utility.
    2. Apply the animation to the category titles (`h2`) in `MenuList.astro`.
    3. (Optional) Use IntersectionObserver in a script to trigger animations as sections enter the viewport.
  </action>
  <verify>
    Categories should gently animate into view as the user scrolls down.
  </verify>
  <done>UX animations refined with smooth fade-in-up transitions.</done>
</task>

<task type="auto">
  <name>Final Polish & Mobile Spacing</name>
  <files>
    - src/pages/menu.astro
    - src/components/Header.astro
  </files>
  <action>
    1. Review vertical spacing on mobile for the H1 and Bento sections.
    2. Ensure the "Prenota" button in the Header feels cohesive with the new menu style.
    3. Optimize image loading (lazy-loading) to ensure page speed remains high.
  </action>
  <verify>
    The page should feel cohesive, fast, and professional on both desktop and mobile.
  </verify>
  <done>Final polish and mobile spacing optimizations completed.</done>
</task>

## Verification Must-Haves
- [ ] Narrative introduction is visible and styled properly.
- [ ] Bento imagery grid displays relevant Mediterranean photos.
- [ ] Category titles animate into view during scroll.
- [ ] Mobile UX is fluid, with no layout shifts.
- [ ] Page feels premium, convivial, and authentic.
