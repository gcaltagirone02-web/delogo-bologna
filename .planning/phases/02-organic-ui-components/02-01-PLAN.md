---
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/pages/menu.astro
  - src/components/MenuList.astro
  - src/components/MenuCard.astro
autonomous: true
requirements:
  - UI-01
  - UI-03
  - STORY-03
---

# Phase 02: Organic UI Components - Plan

This phase focuses on replacing the "tech-heavy" glassmorphism components with organic, custom-feel alternatives, specifically targeting the category navigation bar and section separators. It also includes mobile UX optimizations for better scrolling and touch interaction.

## Task XML

<task type="auto">
  <name>Redesign Category Navigation (Organic Pill)</name>
  <files>
    - src/pages/menu.astro
    - src/styles/global.css
  </files>
  <action>
    1. Remove the "Pillow Liquid Glass" styles from `#category-nav` and `.category-pill` in `src/pages/menu.astro`.
    2. Define a new organic navigation style in CSS:
        - Container: Semi-transparent "Bianco Carta" with a subtle organic border.
        - Active Pill: Implement a "watercolor brush stroke" effect using a custom SVG filter or a pseudo-element with an irregular shape (8-value border-radius).
        - Active Color: Change text to Blu Egeo (#4B9CD3).
    3. Update the `id="category-nav"` to use these new styles.
  </action>
  <verify>
    The navigation bar should look like a soft paper strip with a watercolor highlight on the active item, moving away from glassmorphism.
  </verify>
  <done>Organic category navigation redesigned and implemented.</done>
</task>

<task type="auto">
  <name>Implement Mobile Navigation Best Practices</name>
  <files>
    - src/pages/menu.astro
  </files>
  <action>
    1. Update the horizontal scroll container in `menu.astro` to include `snap-x snap-mandatory`.
    2. add `snap-center` to each `.category-pill` anchor.
    3. Implement "Fade Edges" using a CSS `mask-image: linear-gradient` on the navigation wrapper to signal scrollability.
    4. Adjust the navigation height on mobile to be more compact.
  </action>
  <verify>
    Scroll the category bar on mobile: items should snap to center, and edges should fade correctly.
  </verify>
  <done>Mobile horizontal scroll optimized with snap and fade effects.</done>
</task>

<task type="auto">
  <name>Add Watercolor Section Separators</name>
  <files>
    - src/components/MenuList.astro
  </files>
  <action>
    1. Replace the geometric line/dot separators in `MenuList.astro` with an organic SVG divider.
    2. Use a wavy watercolor-style path or a minimalist olive branch line-art (referencing research).
    3. Ensure the color is consistent with the brand palette (Blu Egeo or Rosso Corallo at lowered opacity).
  </action>
  <verify>
    Visual check between menu categories: geometric lines should be replaced by organic separators.
  </verify>
  <done>Organic watercolor-style separators implemented between menu categories.</done>
</task>

<task type="auto">
  <name>Apply Soft/Irregular Edges to UI</name>
  <files>
    - src/components/MenuCard.astro
    - src/styles/global.css
  </files>
  <action>
    1. Apply a very subtle irregularity to `MenuCard` edges (e.g., using `clip-path` with 1-2px variations or an irregular border-radius).
    2. Ensure the "Organic divider on hover" in `MenuCard` uses a wavy path instead of a straight line.
  </action>
  <verify>
    Menu cards should feel slightly less "perfectly digital" without sacrificing layout stability.
  </verify>
  <done>Soft, non-geometric edges applied to key UI elements.</done>
</task>

## Verification Must-Haves
- [ ] Category navigation replaced glassmorphism with "organic pill" (watercolor highlight).
- [ ] Mobile navigation features snap-center and fade edges.
- [ ] Section separators are now organic SVG paths (wavy/olive).
- [ ] UI elements (cards/pills) have non-perfectly-geometric edges.
- [ ] Mobile touch targets remain >= 44px.
