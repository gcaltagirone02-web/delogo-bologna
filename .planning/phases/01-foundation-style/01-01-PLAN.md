---
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/pages/menu.astro
  - src/layouts/Layout.astro
  - src/components/MenuList.astro
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - PACE-01
  - UI-02
---

# Phase 01: Foundation & Style - Plan

This plan establishes the brand-aligned visual foundation for the menu page by integrating the Marcellus font, applying the "Bianco Carta" textured background, and refining color/spacing patterns.

## Task XML

<task type="auto">
  <name>Install and Integrate Marcellus Font</name>
  <files>
    - src/styles/global.css
    - src/layouts/Layout.astro
  </files>
  <action>
    1. Install @fontsource/marcellus using npm.
    2. Import '@fontsource/marcellus' in `src/layouts/Layout.astro` (or the root layout).
    3. Update `src/styles/global.css` theme to include `--font-marcellus: "Marcellus", serif;`.
    4. Replace `--font-serif` with Marcellus or add it as a new utility. (According to research, we want it for category headers).
  </action>
  <verify>
    Check that Marcellus font is loaded in the browser and applied to utility classes.
  </verify>
  <done>Marcellus integrated into Tailwind theme and global layout.</done>
</task>

<task type="auto">
  <name>Implement Bianco Carta Background with Texture</name>
  <files>
    - src/styles/global.css
    - src/pages/menu.astro
  </files>
  <action>
    1. In `src/styles/global.css`, define a utility class `.bg-paper` (or update `bg-background`) using the SVG noise filter pattern from research.
    2. Adjust `bg-background` variable to #FFFFFF if not already.
    3. Apply the texture utility to the main container in `src/pages/menu.astro`.
  </action>
  <verify>
    Visual check: The page background should have a subtle grain/texture instead of being flat white.
  </verify>
  <done>Textured "Bianco Carta" background applied to the menu page.</done>
</task>

<task type="auto">
  <name>Apply Typography and Color Styling</name>
  <files>
    - src/pages/menu.astro
    - src/components/MenuList.astro
  </files>
  <action>
    1. Update the `<h1>` in `menu.astro` to use `font-marcellus` and `text-accent` (Rosso Corallo #D14638).
    2. Update the `<h2>` in `MenuList.astro` to use `font-marcellus`, `text-accent`, and ensure it's `text-center`.
    3. Update body text and prices to use `text-[#2F4F4F]` (Grigio Ardesia Scuro) consistently.
    4. Apply `tracking-wide` to Marcellus titles for the "web-to-print" feel.
  </action>
  <verify>
    Headers should be Rosso Corallo and use the Marcellus serif font. Body text should be dark slate.
  </verify>
  <done>Typography and brand colors applied to headers and body content.</done>
</task>

<task type="auto">
  <name>Optimize Spacing and Rhythm</name>
  <files>
    - src/pages/menu.astro
    - src/components/MenuList.astro
  </files>
  <action>
    1. Increase vertical padding between sections in `MenuList.astro` (e.g., from `mb-12 md:mb-24` to a more generous spacing like `py-20 md:py-32`).
    2. Ensure section headers have relaxed margins.
    3. Verify that line-heights for dish descriptions in `MenuCard` (if applicable) use `leading-relaxed`.
  </action>
  <verify>
    The page should feel more "airy" and easier to read, with clear separation between categories.
  </verify>
  <done>Relaxed vertical rhythm and spacing implemented across the menu.</done>
</task>

## Verification Must-Haves
- [ ] Marcellus font is correctly applied to all <h1> and <h2> headers.
- [ ] Background is "Bianco Carta" with a visible (but subtle) texture.
- [ ] Category headers are colored Rosso Corallo (#D14638).
- [ ] Spacing between categories has been significantly increased to improve "reading rhythm".
